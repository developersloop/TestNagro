// eslint-disable-next-line no-unused-vars

const state = {
  
};

const mutations = {
  
};

const actions = {
    
};

const getters = {
    
}


export const Propriedades = {
    namespaced:true,
    state,
    mutations,
    actions,
    getters
    
}